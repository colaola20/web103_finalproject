services:
  app:
    build: .
    container_name: fun-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=your_postgresql_connection_string_here
      - OPENAI_API_KEY=your_openai_api_key_here
      # Add other backend .env variables here